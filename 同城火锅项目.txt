








=======================================================
文件夹  media（图片）、templates、db_tools（python脚本，数据库初始化）
apps 包，用于保存所有的app     users拖进来
extra_apps包，用于存放第三方包，如果源码修改时，放这里，就不用放虚拟环境里。
以上这俩包，mark source route  后面input方便。
在settings里加入python的跟搜索路径，这样的话，导users包就可以直接import users：

sys.path.insert(0, BASE_DIR)
sys.path.insert(0, os.path.join(BASE_DIR, 'apps'))
sys.path.insert(0, os.path.join(BASE_DIR, 'extra_apps'))


=============================================
为了用户操作的死循环，把user的操作单独放一个app--useraopration，eg：收藏

新建app：
>python manage.py startapp goods   trade   user_operation 拖进apps 
settings 加进去
实际开发中，任何models都应该添加的add_time  update_time  

-------------------------------------
users-----models.py
from django.contrib.auth.models import AbstractUser
from datetime import datetime

class UserProfile(AbstractUser):

【年龄不设字段，因为年龄是动态值，直接设出生日期，咱们可以算】

写完之后在settings里设置，才能真正替换系统自带的users
AUTH_USER_MODEL = 'users.UserProfile'

验证码保存操数据库中，或者redis中
保存到数据库中：
class VerifyCode(models.Model):

------------------------------------
把xadmin 和 DjangoUeditor  复制到extra_apps
INSTALLED_APPS = [
    'xadmin',
    'DjangoUeditor',
]

goods_desc = UEditorField   富文本
----------------------------------------
trade  购物车：结算就清空  数量加减

from django.contrib.auth import get_user_model
User = get_user_model()    # 就不用 from users.models import UserProfile
#get_user_model() 这个函数直接实现从配置的路径中找


订单
# trade_no 支付宝支付，生成订单号与本系统订单形成关联。第三方。
# pay_status 订单支付状态。用户可能支付一半就不支付了


===================================================







































































































































































































































































































































